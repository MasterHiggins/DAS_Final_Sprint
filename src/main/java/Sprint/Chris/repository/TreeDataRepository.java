package Sprint.Chris.repository;

import Sprint.Chris.model.TreeData;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TreeDataRepository extends JpaRepository<TreeData, Long> {
}
